--in general


--eac3To Form
*validation



--AviSynth Form
*validation


--x264 Form
*validation

--bluray title info form
*validation


--parsers
*validation
*error handling
*hard coded languages
*hard coded audio types


--unit tests
*unit test new helper function methods
*unit test extension methods


--1.8 todo
*eac3to id/feature "#)" removed from header text on create eac3to batch file screen summary grid, which allows for sorting on playlists
*Added a unit test to support the functionality that removes the eac3to id/feature from the BluRaySummaryInfo Header Text
*Updated versions in class libraries to 1.8
*Changed BluRaySummaryInfo.Id to Eac3ToId, for better code readability
*Changed Setting class property Path to Value to be more of a Name/Value Pair
*Last directory user selected to create x264 batch file is now being saved in BatchGuy settings and will be the default directory for the popup dialog
*Last directory user selected to save avisynth files is now being saved in BatchGuy settings and will be the default directory for the popup dialog
*Last directory user selected to save x264 batch file is now being saved in BatchGuy settings and will be the default directory for the popup dialog
*Removed setting the default directory for all the other select path/file dialog popups 
*Create eac3to batch file summary grid header text changed from "Header" to "Playlist / Videofile / Runtime"
*Made some small cosmetic changes based on user feedback

*Re-named IBatchFileWriteService to IEAC3ToBatchFileWriteService
*Re-named EAC3ToBatchFileWriteService to EAC3ToBatchFileWriteService
*Created new EAC3ToBatchFileWriteWarningService to notify the user of any warnings before attempting to create eac3to batch file
*Added unit tests to support the new EAC3ToBatchFileWriteWarningService service


*Let user know settings structure has changed and the old settings will not work


--upcoming
*figure out a better solution to inform of the status of long running tasks
*find where you have number of files hardcoded to 99 and put the real value in it
*ability to create sample.avs/sample.encode.bat
*Bug where you can't close title info screen because episode required even though nothing is chosen.  Button works but not the x at the top [couldn't reproduce]
*ability to save/load eac3to batch guy data

