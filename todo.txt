--in general
*introduce logging


--eac3To Form
*validation
*de-couple bat path from folder where files are stored path
*hardcode episode (ie 99), so you cannot do series greater than 100 episodes


--AviSynth Form
*add a filter dropdown box so multiple filters can be used.
*validation


--x264 Form
*validation

--bluray title info form
*validation
*use serialization or some other mechanism to do undo/cancel


--parsers
*validation
*error handling
*hard coded languages
*hard coded audio types


--unit tests
*unit test new helper function methods
*unit test extension methods


--1.6 todo
*Added -log paramter to eac3to parameters -done
*BluRay Disc info object no longer has its own eac3to configuration.  This is global for all discs and the eac3to configuration has been pulled out -done
*Fixed bug where eac3to output directory select directory dialog had duplicate verbiage in the title -done
*User now Drag and Drop blu-ray folders onto the disc grid on the eac3to screen -done
*Added a lot more log4net logging and user friendly error messages.  Also created a global error format template -done




*allow the ability to name output folders/track names
For this, you will make a new service call EAC3ToOutNamingService and EAC3ToOutputService will use it
The Big Bang Theory S08E01 1080p Remux AVC FLAC 5.1-VietHD.mkv


*Maybe put some yes/no when user presses buttons that launches processes
*Maybe keep track of the Blu-ray folder directory, so you will open up the same directory every time
*Add ffvideosource/dss drop down to x264 screen and the ability to add video filter args
*ability to save/load eac3to batch guy data
*ability to created sample.avs/sample.encode.bat



--upcoming
*remux screen, drag/drop or multi-select the files for remux 
*remux focus ie allow folder names other than #
*mkvmerge integration
*look into why the busy mouse indicator doesn't seem to work
*put the bluray title on the title info page

d1 = 4)
d2 = 4)
d3 = 3)