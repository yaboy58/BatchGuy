--in general
*introduce logging


--eac3To Form
*validation
*de-couple bat path from folder where files are stored path
*hardcode episode (ie 99), so you cannot do series greater than 100 episodes


--AviSynth Form
*add a filter dropdown box so multiple filters can be used.
*validation


--x264 Form
*validation

--bluray title info form
*validation
*use serialization or some other mechanism to do undo/cancel


--parsers
*validation
*error handling
*hard coded languages
*hard coded audio types


--unit tests
*unit test new helper function methods
*unit test extension methods


--1.7 todo
*User receives error message if encode names on create x264 batch file screen don't have (.mkv) extension
*User receives error message if encode names on create x264 batch file screen are not unique
*Fixed bug where x264 template script text was not being trimmed for spaces and causing problems with the x264 batch file
*Fixed bug where if you enter eac3to audio arguments on the title info screen, save and the come back and select the same audio, the existing arguments are cleared
*PCM audio will default to FLAC
*User can now set episode name on eac3to screen when extracting for remux
*TrueHD audio will default to AC3 with -640 argument
*Fixed bug where if you copy/paste encode name on x264 batch file create screen that has line breaks, only the 1st line would be pasted
*Fixed bug where the busy icon for the mouse cursor isn't set when the application performs long running tasks





--upcoming
*Bug where you can't close title info screen because episode required even though nothing is chosen.  Button works but not the x at the top [couldn't reproduce]
*ability to create sample.avs/sample.encode.bat
*ability to save/load eac3to batch guy data
*Maybe keep track of the Blu-ray folder directory, so you will open up the same directory every time
*find where you have number of files hardcoded to 99 and put the real value in it
*put the bluray title on the title info page


